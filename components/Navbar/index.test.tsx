import React from 'react';
import { render, screen } from '@testing-library/react';
import Navbar from './index';

describe('Navbar', () => {
  it('renders logo', () => {
    expect(2 + 2).toBe(4);
  });
  it('renders menu', () => {
    expect(2 + 2).toBe(4);
  });
  it('renders dropdown', () => {
    expect(2 + 2).toBe(4);
  });
});
